name: Verify services sync

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify-services-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify README services match config
        run: python3 scripts/verify_services_sync.py
